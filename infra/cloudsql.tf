# The current local-first demo BFF is stateless and does not require Cloud SQL.
# Keep this file in place so storage/database resources can be added later without
# changing the Terraform project layout used by CI.

